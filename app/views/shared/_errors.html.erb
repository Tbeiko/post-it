<% if entry.errors.any? %>
  <div class='alert alert-error'> 
    <h5>Something isn't right:</h5>
    <ul>
      <% entry.errors.full_messages.each do | message| %>
        <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>