<%= render 'shared/content_title', title: "All Posts" %>

<% @posts.each do |post| %>
  <div class="row">
    <div class='span8 well'>

      <%= render post.categories %>

      <h4><%= link_to post.title, post.url %></h4> <!-- This is not working -->
      <p><%= post.description %></p>
      <p>
        <span class='quiet'>posted by</span> <%= post.creator.username %> <!-- This isn't working either -->
        <small>at <%= post.created_at %></small>
      </p>
      <small>
      <span>
        <%= link_to("#{post.comments.count} comments", post_path(post)) %>
      </span>
      <span>
        (<%= link_to 'edit', edit_post_path(post) %>)
      </span>
      </small>
    </div>
  </div>
<% end %>































